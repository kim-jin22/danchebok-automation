# progress.md — 진행 로그 (하네스 ④ 지식 저장소)

> 하네스 방법론의 "지식 저장소". 무엇을 만들었고, 어떤 설계 결정을 왜 내렸는지 남긴다.
> 세션이 길어져 맥락이 흐려지면 작업지시서(`docs/작업지시서.md`)와 함께 다시 읽힌다.
> ※ 익명 사례 연구 — 실제 사명·거래처·단가·실명은 모두 제거됨.

## 프로젝트

단체복 커머스의 반복업무 사고를 예방하는 자동화 시스템. 단일 HTML 인터랙티브 프로토타입, 가상·익명 샘플 데이터.

## 완료 현황

| 영역 | 내용 | 상태 |
|---|---|---|
| 대시보드 | 담당팀별 상태 현황(막대 차트) · 상태 분포 도넛 · 임박 · 상담 사유별 | ✅ |
| 상세주문내역 | 주문정보 / 메모·검증 / 주문옵션(사이즈·인쇄정보), 단가 수정→견적 자동 재계산 | ✅ |
| 접수 자동 분류 | 14단계 트리아지(실시간) → 정상 이관 / 문제 상담 자동 분류 | ✅ |
| 진행단계 자동 처리 | 관문 통과 자동 진행 / 문제 자동 보류·플래그, 보류 목록 필터 | ✅ |
| M2 게이트 | 진행상태 선택 시 조건 자동 판정, 미충족 시 이동 차단·보정 | ✅ |
| M3 견적 검증 | 표준단가 대비 자릿수 자동 감지 | ✅ |
| 도메인 사고 방지 | 나염 판 만료 · 자수 회유/판보유 · 추가건 진위 · 위치↔크기 교차검증 | ✅ |
| 검증(감사) 탭 | 통과 근거 로그 + 현재 상태 진입조건 재검증 | ✅ |
| 사고 예방 맵 | 사고 7종 → 방어 장치, 카드 클릭 시 해당 화면으로 이동 | ✅ |

## 핵심 설계 결정

- **칸반 폐기 → 실무형 구조 채택.** 대량 주문에서 카드벽은 가독성이 붕괴되고 "담당자 공간" 의미가 사라짐. 담당팀별 구획 + 상태별 건수/차트 + 필터 리스트로 전환. 구획 경계 = 핸드오프(S1) 사고 지점.
- **표준 상태:** 미처리 → 상담진행 → 시안요청 → 시안등록 → (수정요청) → 결제요청 → 입금완료 → 상품준비중 → 발송완료. 인쇄 없는 건은 시안 단계를 건너뜀.
- **미처리 = 접수 버퍼.** 자동 분류가 즉시 비운다. 사람이 실제로 붙는 단계는 상담진행(예외)·시안요청·결제요청·상품준비중으로 좁혀짐.
- **버튼 없는 상시 자동화.** 접수 분류·진행 처리는 실행 버튼 없이 실시간 동작. 실시간 로그로 표현.

## 인쇄 비용 구조 (실무 매뉴얼 기준)

- 인쇄방법 5종: 나염·전사·패딩나염·모자나염·자수.
- 가격 = 인쇄방법 × 크기(소/대/특대) × 수량. 나염류는 임계 수량 미만=판비(일괄)/이상=장당 구조.
- 비용 3분리: 장당 단가 × 수량 + 일괄(판비) + 기타비용 → 금액 자동 계산. 방법/크기 변경 시 표준단가 자동 적용.
- M3 자릿수 검증: 실효 장당(=금액/수량)을 표준 실효 장당과 대조 → 판비형/장당형 모두 커버.

## 자동화 유형 (종합)

1. **계산 자동화** — 매뉴얼 표준단가로 견적 자동 산출.
2. **검증 자동화** — 관문 조건·자릿수를 주문 데이터로 자동 판정(M2/M3).
3. **분류 자동화(트리아지)** — 접수 14단계 검증 → 자동 이관/상담. fail-safe(애매하면 상담).
4. **일괄 실행 자동화** — 전 주문 스캔 → 통과 자동 진행/문제 자동 보류.
5. **이벤트 자동화** — 담당자 지정 시 안내 SMS 자동 발송, 게이트 보정 완료 시 상태 자동 변경.

## 상담 자동화 (도메인 경험 반영)

- 필드 간 모순 교차검증: 인쇄유무 ↔ 인쇄내용, 인쇄위치 ↔ 인쇄크기(좌흉=소형·등판=특대…).
- 나염 판 보관기한: 추가건인데 원본이 기한 초과면 판 재제작·판비 재부과 경고(장당만 청구 시 손실 방지).
- 소량 자수 회유(전사·나염 권장), 자수 판 보유 재주문 판비 면제, 추가건 진위(원본 인쇄내용 대조).
- 상담진행은 걸러진 사유를 태그·사유별로 분류해 예외를 바로 처리 가능.

## 하네스 흔적 3종

1. **맥락 고정** — 작업지시서/매뉴얼을 단일 진실 소스로 (`docs/작업지시서.md`).
2. **재사용 스킬** — 관문 자동판정 `checkGate()/evalGate()`, 자릿수 검증 `validateQuote()`, 표준단가 산출을 독립 함수로 분리.
3. **역할 분리** — 기획 / 화면 / 검증 / 게이트를 컨텍스트 단위로 나눠 작업.

## 검증

- 전 로직 자동 검증(jsdom) 통과, 콘솔 에러 0.
- 데이터 무결성: 견적 = 의류비 + 인쇄비, 사이즈 합 = 수량, 인쇄X는 시안 단계 미포함 — 불일치 0.

## 로드맵

- 디자이너 영역 AI 옷 미리보기(고객이 위치·크기 직접 확인).
- 외부 이벤트 연동(입금·파일 수신 웹훅으로 자동 실행), 실데이터·권한·감사 로그 연계.
